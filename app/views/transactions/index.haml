%table#transactions
  - @transactions.each do |transaction|
    %tr
      %td= transaction.transaction_date.strftime('%m/%d/%Y')
      %td= transaction.receiver
      %td= transaction.category.name
      %td= "#{number_with_precision(transaction.amount_value, precision: 2)}"
      %td= button_to 'delete', account_transaction_path(transaction.account, transaction), method: :delete, data: { confirm: 'Are you sure?' }