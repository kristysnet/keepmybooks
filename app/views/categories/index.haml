%h1 Categories
%div= link_to 'new', new_category_path()
%table#categories
  - @categories.each do |category|
    %tr
      %td= category.name
      %td= link_to 'show', category_path(category)
      %td= link_to 'edit', edit_category_path(category)