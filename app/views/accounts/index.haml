%h1 Accounts
%div= link_to 'new', new_account_path()
%table#accounts
  - @accounts.each do |account|
    %tr
      %td= account.name
      %td= link_to 'show', account_path(account)
      %td= link_to 'edit', edit_account_path(account)